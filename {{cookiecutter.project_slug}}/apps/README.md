# apps/

Project-specific Django apps go here, e.g. `apps/core/`, `apps/billing/`.
Add each one to `INSTALLED_APPS` in `{{ cookiecutter.project_slug }}/settings.py`
alongside the `django_fundamentals`-provided apps.
