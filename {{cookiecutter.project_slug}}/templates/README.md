# templates/

Project-level template overrides go here. To override a
`django_fundamentals` template (e.g. `django_fundamentals/base.html`),
create a file at the same relative path here — this directory is listed
first in `TEMPLATES[0]["DIRS"]`, so it takes precedence.
